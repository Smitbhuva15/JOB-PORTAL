apiVersion: v1
kind: Service

metadata:
  name: joblinker-backend-service

spec:
  selector:
    app: joblinker-backend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: LoadBalancer
